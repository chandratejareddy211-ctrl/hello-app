public class HelloAppUC3 {
    public static void main(String[] args) {
        String ;name

        if (args.length > 0) {
            name = args[0];
        } else {
            name = "Chandra"; 
        }
        System.out.println("salam valekum, " + name + "!");
    }
}
